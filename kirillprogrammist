#include <iostream>
#include <ctime>
#include <cstdlib>
#include <utility>
#include<stdlib.h>

using namespace std;

int zxc (int &c, int &mass[c], int &q) {
	int k;
		for (int i =0; i<size; i++) {
		if (mass[c] == q)
		k = i;
	}
	cout <<endl<< "Â ìàññèâå íàéäåí ýëåìåíò  èíäåêñîì: " << k;
}

int main() {
	
	setlocale(LC_ALL, "rus");
	
	srand(time(NULL));
	
	int const size = 20;
	int arr[size];
	
	for (int i =0; i<size; i++) {
	arr[i]=(rand()%100)-50;
	}
	
	for (int i =0; i<size; i++) {
	cout << arr[i] << " ";
	}
	
	int q;
	
	cout << endl << "Ââåäèòå ýëåìåíò: ";
	cin >> q;
	zxc(*size, *arr[size], *q); 
}
